@model IEnumerable<CategoryViewModel>

<br />
<div class="row">
    <div class="col-6">
        <h2 class="text-primary">Categories List </h2>
    </div>
    <div class="col-6 text-right">
        <a class="btn btn-primary" asp-area="Admin" asp-controller="Category" asp-action="AddCategory">
            <i class="fas fa-plus"></i>
            &nbsp; Add Category
        </a>
    </div>
</div>

<br />
<div class="p-4 border rounded">
    <table id="tblData" class="table table-striped table-bordered" style="width:100%">
        <thead class="thead-dark">
            <tr class="table-info">
                <th>ID</th>
                <th>Category</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var category in Model)
            {
                <tr >
                    <td>@category.Id</td>
                    <td>@category.Title</td>
                    <td>
                        <a asp-area="Admin" asp-action="SubCategories" asp-controller="Category" asp-route-id="@category.Id" class="btn btn-block btn-outline-info btn-sm">Sub Categories</a>
                        <a asp-area="Admin" asp-action="Edit" asp-controller="Category" asp-route-id="@category.Id" class="btn btn-block btn-outline-info btn-sm">Edit</a>
                        <form asp-area="Admin" asp-action="Delete" asp-controller="Category" method="post">
                            <input name="id" type="hidden" value="@category.Id" />
                            <button type="submit" class="btn btn-block btn-outline-danger btn-sm">Delete</button>
                        </form>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>

