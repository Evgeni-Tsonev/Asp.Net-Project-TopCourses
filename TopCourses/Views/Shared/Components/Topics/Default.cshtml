@using TopCourses.Core.Models.Topic
@model ICollection<AddTopicViewModel>

@if (Model.Count == 0)
{
    <h1>Prazno</h1>
}
else
{
    foreach (var topic in Model)
    {
        <h1>@topic.Title</h1>
    }
}


